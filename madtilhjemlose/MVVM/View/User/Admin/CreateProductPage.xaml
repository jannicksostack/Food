<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="madtilhjemlose.MVVM.View.User.Admin.CreateProductPage" >
    <VerticalStackLayout Margin="50, 0" MaximumWidthRequest="500" Spacing="10">
        <Label Text="Create Product" FontAttributes="Bold" FontSize="24" VerticalTextAlignment="Center" HorizontalTextAlignment="Center"/>
        <Image Source="{Binding ImagePath}" HeightRequest="150"/>
        <Button Text="Vælg fil.." Command="{Binding ChooseFileCommand}" WidthRequest="200"/>
        <Entry Placeholder="Navn" Text="{Binding Name}"/>
        <Entry Placeholder="Type" Text="{Binding Type}"/>
        <Button Text="Create" Command="{Binding CreateCommand}" />
    </VerticalStackLayout>
</ContentPage>